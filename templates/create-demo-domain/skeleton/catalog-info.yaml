apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: demo-${{ values.domain_id }}
  title: ${{ values.display_name }}
  description: >
    Customer demo extending general-simulation (${{ values.upstream_branch }} @ ${{ values.upstream_sha }}).
    Generated by lab-gen-sim workshop template.
  tags:
    - general-simulation
    - demo
spec:
  type: service
  lifecycle: experimental
  owner: user:default/robertsandoval
  dependsOn:
    - component:default/general-simulation-upstream
